/** Persisted when user finishes the last step (Done). */
export const APP_TOUR_STATUS_KEY = "valyou.appTour.status.v1";

/** Same key as RiskDisclosureGate — tour auto-offer waits until accepted. */
export const RISK_DISCLOSURE_ACCEPTED_KEY = "valyou.riskDisclosureAccepted.v1";

export const APP_TOUR_RISK_ACCEPTED_EVENT = "valyou-risk-accepted";
